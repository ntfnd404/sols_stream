# streaming

HLS playback and streaming runtime.

Owns HLS source/auth modeling, platform support checks, playback controller
lifecycle, status, and disposal. UI widgets remain thin controls around this
package API.
