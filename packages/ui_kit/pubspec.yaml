name: ui_kit
description: sols.stream design system — tokens, typography, theme
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
