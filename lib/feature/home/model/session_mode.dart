/// Product-level session type serialized as the Home route `intent`.
///
/// `stream` is reserved by the URL contract but remains unavailable until its
/// product and transport semantics are specified.
enum SessionMode { p2p, stream }
